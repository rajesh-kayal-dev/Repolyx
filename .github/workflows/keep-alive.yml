name: Keep Alive

on:
  schedule:
    - cron: "*/14 * * * *"

jobs:
  ping:
    name: Ping Render Server
    runs-on: ubuntu-latest
    steps:
      - name: Ping Health Endpoint
        run: curl -s -o /dev/null -w "%{http_code}" https://repolyx-server.onrender.com/api/health
